# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# scratch / runtime state (default CGC_STATE_DIR)
/tmp/cgc/
*.answer.txt

# local tool state (session/telemetry files with machine-local paths) — never ship
.omc/

# local env / secrets — never commit
.env

# OS
.DS_Store
Thumbs.db

# install backups
*.bak
.claude/worktrees/
